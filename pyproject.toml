[project]
name = "oclite"
version = "0.2.5"
description = "Lightweight OpenClaw-compatible gateway/runtime with Telegram routing and a minimal control UI."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
oclite = "oclite.cli:main"
